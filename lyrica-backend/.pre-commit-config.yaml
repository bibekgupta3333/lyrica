# Lightweight pre-commit config for development
# To skip hooks: git commit --no-verify

repos:
  # Basic checks only (fast and non-intrusive)
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: ["--maxkb=5000"]
      - id: check-merge-conflict

  # Auto-formatting (will fix issues automatically)
  - repo: https://github.com/psf/black
    rev: 24.1.1
    hooks:
      - id: black
        language_version: python3.12
        args: ["--line-length=100"]

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: ["--profile", "black", "--line-length=100"]

  # Linting (relaxed - warnings only, won't block commits)
  # Note: Commented out to make commits faster during development
  # Uncomment for stricter checks before production

  # - repo: https://github.com/pycqa/flake8
  #   rev: 7.0.0
  #   hooks:
  #     - id: flake8
  #       args: ["--config=.flake8"]

  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       additional_dependencies:
  #         - types-requests
  #         - types-PyYAML
  #         - sqlalchemy[mypy]
  #       args: [--ignore-missing-imports, --no-strict-optional, --warn-no-return]
